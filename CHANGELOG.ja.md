# 変更履歴

[English](CHANGELOG.md) ・ 日本語

版ごとの変更を人が読む用にまとめたもの。それぞれの機能の説明は
[README.ja.md](README.ja.md)、細かい経緯は `git log` にある。

版番号は `Cargo.toml` の `[workspace.package]` にひとつだけあり、5 つのクレート
全部がそれを共有する。日付は版を切った日。

## 0.6.2 — 2026-09-02

### 修正

- Processing の数値による `rectMode(3)`、`colorMode(3)` の既定範囲、
  `fill(-1, alpha)` を Java Mode と同じ意味で描くようにした
- p5.js の `filter(BLUR)` と、配列の負数キーを使う疎な二次元配列に対応した

## 0.6.1 — 2026-09-02

### 修正

- インポート画面の名前・作者・タグ列が左端へ圧縮されず、利用可能な横幅を使うようにした

## 0.6.0 — 2026-08-30

### 追加

- FragCoord.xyz の短縮記法 **GOLF** (`f3`、`@(N)`、`nor`、`R` `T` `C` `O`、
  空の構築子引数、`;` の省略、`**`、`~`、`#D` …) を見分け、コンパイル前に
  つぶやきGLSL へ展開する。XorDev 氏の投稿がそのまま動く。先頭の題名行は
  読み飛ばす
- FragCoord.xyz の `void main()` 形式のシェーダー: `u_resolution` `u_time`
  `u_mouse` `u_frame` `fragColor` を FragCoord と同じく供給し、重ねて書かれた
  `uniform … u_resolution;` 宣言は無視する
- ギャラリーの複数選択 (`Ctrl`+クリック、`Shift`+クリック、`Ctrl`+`A`) と、
  印を付けた作品の **エクスポート** (`X`)。ソース・名前・作者・リンク・タグ・
  お気に入りを 1 つの `*.tsubu.json` に書き出す
- **インポート** (`I`): エクスポートファイルを開き、名前・作者・タグを見て
  取り込むものを選ぶ。同名の作品は別名で追加する
- 設定 → データ → **保存場所**: OS のフォルダ選択でデータ領域を移す。
  `config.json` に記録し、次回起動から使う
- ギャラリーで `Ctrl`+`C` / `Ctrl`+`V`: 印を付けた作品をエクスポート形式の JSON で
  コピーし、貼り付けて複製する (同名は別名)。ただの文字列を貼れば、それを
  ソースにした新しい作品になる
- 印がある状態の `Delete` は、一度の確認で印の作品を全部消す
- 依存に `rfd` (OS のファイル・フォルダ選択) と `arboard` (クリップボード) を追加

### 修正

- 言語「システムに合わせる」が Windows のユーザー表示言語
  (`GetUserDefaultLocaleName`) を引くようにした。これまでは Windows に無い
  `LANG` 系の環境変数だけを見ていたため、常に英語になっていた

## 0.5.1 — 2026-08-23

### 修正

- 狭いウィンドウで Gallery のショートカット説明がアプリ名に重ならないようにした

### 変更

- イベントループを作品の次フレーム、入力、各種タイマーまで待機させ、一時停止中や
  低 FPS 設定で VM 実行、図形生成、GPU 転送、UI 再描画を繰り返さないようにした
- 自由形状の三角形・四角形を固定長の専用経路で描き、`beginShape()` の頂点・曲線
  バッファをフレーム間で再利用するようにした

## 0.5.0 — 2026-08-23

### 追加

- P3D / WEBGL の `point(x, y, z)`、`line(x1, y1, z1, x2, y2, z2)`、
  `vertex(x, y, z)` に対応し、自由形状でも頂点の奥行きを保つようにした
- `mag(x, y, z)`、3 次元の `dist()`、平均・標準偏差を取る
  `randomGaussian(mean, sd)` に対応した

### 修正

- 作品ファイルの保存をアトミックな置換にし、名前変更時に古いファイルを
  削除できなかったときは新しいファイルをロールバックするようにした
- サムネイル生成を複数の UI フレームへ分割し、PNG の読み書きをワーカー
  スレッドへ移して、大きな Gallery でも操作を止めにくくした
- Linux のタイトルバーボタン `menu` に対する無害な `sctk_adwaita` 警告を
  表示しないようにした
- GPU キャンバステストでデバイスを共有し、Linux で並列初期化したときの
  ドライバークラッシュを避けた
- p5.js の未対応な 3D / UV オーバーロードへ渡した引数を黙って切り捨てず、
  対応していない形式として停止するようにした。展開引数でも同じ診断になる
- 実行予算の一時超過以外の実行時エラーを 3 フレーム繰り返さず、最初の発生で
  停止するようにした

### 変更

- Viewer の作品ごとの実行状態、フレーム数、時計、キャンバス属性を一体化し、
  追加・削除時に対応関係がずれない構造にした
- リポジトリの読み取り失敗をログへ残し、Gallery の絞り込み候補を空の値で
  上書きしないようにした

## 0.4.2 — 2026-08-22

### 修正

- **sketch 59 のような FragCoord / ShaderToy 形式の GLSL が動かなかった。**
  `mainImage`、`iTime`、`iResolution` を twigl の実行環境へ対応付け、naga が
  検証できなかった vec4 1 本からの `mat2` 構築も互換変換するようにした

## 0.4.1 — 2026-08-22

### 追加

- Windows インストーラ作成スクリプトへ `-Sign` を追加。`CODESIGN_CERT` が選ぶ
  証明書で実行ファイル、インストーラ、アンインストーラをすべて署名する

### 修正

- **初期値を省いたループ変数が入れ子にあるつぶやき GLSL の表示が崩れていた。**
  naga がブロック変数を WGSL 関数の先頭へ持ち上げるため、外側のループが次の周へ
  入っても内側の `for(int i; ...)` が前回値を引き継いでいた。twigl 流の短縮した
  シェーダーが前提とするゼロ初期化を、ループへ入るたび補うようにした

## 0.4.0 — 2026-08-19

### 追加

- **つぶやきGLSL も整形できるようになった。** これまでは `⌘F` / `⌘K` を押しても
  何も起きなかった。文の区切りも括弧の対応も C 系で共通なので、方言を見分ける
  必要はなかった。`#version` のような行と `#つぶやきGLSL` のタグだけは
  行ごとそのまま通す
- p5.js で文字列を展開できる。`[...'#つぶやきProcessing']` は 15 要素の配列
- `p5.Vector` の静的メソッド。`random2D` `random3D` `fromAngle` `add` `sub`
  `mult` `div` `lerp` `cross` `normalize` `dot` `dist` `mag` `angleBetween`。
  インスタンスのメソッドと違い、**渡したベクトルは変えず**新しいものを返す
- 変更履歴 (`CHANGELOG.md` / `CHANGELOG.ja.md`) を置いた
- macOS 用の `app/assets/icon-1024.png`。0.3.6 の macOS インストーラが
  これを探していたが、`scripts/make-icon.py` は描いていなかった

### 修正

- **Windows のタスクバーにアイコンが出なかった。** 原因は 2 つあった。
  winit の `with_window_icon()` が設定するのは `ICON_SMALL` (タイトルバー)
  だけで、タスクバーが見る `ICON_BIG` は渡さないと空にされる。そして
  設定したところで、窓が現れた直後の explorer の問い合わせに間に合わない。
  起動処理がメッセージループを塞いでいるあいだに見限られ、二度と訊かれないので、
  1 フレーム描き終えてから貼り直す
- **組み込みをコールバックに渡すと壊れていた。** `[1,4,9].map(sqrt)` の形。
  作品の関数と違って組み込みはフレームを積まないので、コールバック 1 回の
  つもりで呼び出し元の続きまで走っていた。`map` `reduce` `sort` の 3 か所
- 整形の空白の入れ方。`2.-r` → `2. - r`、`9./dot(p,p)` → `9. / dot(p, p)`、
  `i++<9` → `i++ < 9`。書いた文字の末尾で判定していたため、小数点と
  プロパティの区切りを見分けられていなかった
- 短縮が `p.x` を `p . x` へ**伸ばしていた**。点を数の一部と同じに扱っていたため

## 0.3.6 — 2026-08-18

### 追加

- **macOS インストーラ (.pkg)。** `scripts/build-macos-installer.sh` が
  release ビルド → .app へ組む → 署名 → .pkg → 公証 → staple まで流す。
  既定で Apple Silicon と Intel の両方を含む universal。できあがりは
  Windows 版と同じ `target/installer/` へ置く

### 修正

- **Gallery で別の作品を編集・削除しただけで、表示中の作品のキャンバスが
  消えていた。** `Graphics` は全作品で 1 つを共有しているので、初期化すると
  `createCanvas()` で決めた大きさが失われ、`setup()` は二度と走らないので
  図形が左上へ寄る。初期化するのは表示中の作品を消したときだけにし、
  その場合は繰り上がってきた作品の状態を戻す

## 0.3.5 — 2026-08-17

### 追加

- p5.js に `switch` / `case` / `default`。`break` を書かなければ次の case へ
  落ちるのは Java Mode と同じ
- `windowWidth` / `windowHeight`。`innerWidth` `innerHeight` `displayWidth`
  `displayHeight` も同じ値を返す。表示領域そのものを指すので、`createCanvas()`
  のあとも変わらない
- `beginShape()` の `QUADS` と `QUAD_STRIP`。4 点ずつを**閉じた**面にする。
  1 つの多角形にまとめると面と面の境目に縁の線が引かれず、帯や網の輪郭が消える
- 文字で書いた色。CSS の色名 147 語と `#rgb` `#rgba` `#rrggbb` `#rrggbbaa`。
  p5.js と同じく `colorMode()` には従わない
- JavaScript の予約語をプロパティ名に書ける (`{default:1}.default`)

### 修正

- **Gallery から初めて Viewer を開くと、隣の作品の `setup()` が 1×1 の大きさで
  走っていた。** 先読みは「最後に描いたときの大きさ」を使っており、Viewer を
  まだ一度も描いていないあいだは初期値のままだった。
  `createCanvas(innerWidth, innerHeight)` と書いた作品はその 1 をグローバルへ
  焼き付け、`setup()` は二度と走らないので、そのセッションのあいだずっと
  真っ黒になる
- 無い関数を呼んだときに名前を出す。`void は関数として呼べません` から
  `createColorPicker() という関数はありません` へ
- `switch` の中の `continue` が外側のループに効くようになった。ループの外では
  従来どおり撥ねる

## 0.3.4 — 2026-08-16

### 追加

- `I` の情報表示に GPU 名と描画経路 (`Vulkan · DiscreteGpu`)。2 枚載っている
  機械でどちらが選ばれたかが分かる
- 全画面で 3 秒操作が途切れるとマウスカーソルを消す。**カーソルがこの窓の上に
  ないときは消さない** — モニタが 2 枚あれば、カーソルは別の画面へ出ていける
- README に画面写真。表記を「つぶやきProcessing」「つぶやきGLSL」に統一
- インストーラのファイル名に OS を入れる
  (`TsubuGallery-0.3.4-windows-x64.exe`)
- `docs/TsubuGallery_Expansion.md` — three.js / Canvas 2D / WGSL / CUDA への
  対応を検討した記録

### 修正

- デバッグビルドが起動直後に落ちていた。egui の `TexturesDelta` を空にしない
  まま捨てており、`debug_assert!` に引っかかっていた

## 0.3.3 — 2026-08-16

### 追加

- アプリのアイコン。`scripts/make-icon.py` が描き、`app/build.rs` が exe へ
  版情報ごと埋める。winit のウィンドウアイコンにもなる
- **Windows インストーラ。** `scripts/build-installer.ps1` が release ビルド →
  (証明書を渡せば) 署名 → Inno Setup 6 まで流す。管理者不要のユーザー単位
  インストールで、配る中身は `tsubugallery.exe` 1 つだけ

## 0.3.2 — 2026-08-16

### 追加

- ウィンドウを作れない / GPU を用意できないときに、理由を **OS のダイアログ**
  で出してから終了する。ショートカットから起動するとコンソールが一緒に閉じて
  標準エラーを読めないため

## 0.3.1 — 2026-08-16

### 追加

- 再生速度 0.25× 〜 4×。`↑` / `↓` と設定から。1 秒に何回描くかを決める
  フレームレートとは別物で、作品にとっての 1 秒が実時間の何秒かを決める
- 作品ごとの時計。GLSL の `t` はこれ。`Space` で止まり、別の作品を見ている
  あいだは進まない
- サムネイルは再生速度によらず 1× 固定で撮る。同じ作品からいつでも同じ絵が
  出るようにするため

## 0.3.0 — 2026-08-16

### 追加

- **つぶやきGLSL。** twigl の geekest 相当のフラグメントシェーダー 1 本を
  そのまま置ける。naga で WGSL へ翻訳して wgpu のパイプラインを組み、画面を
  覆う三角形 1 枚に貼る。`r` `t` `f` `m` `FC` `o` と `rotate2D` `hsv`
  `snoise2D` などは前置きとして与えるので宣言せずに使える
- 3 つの方言 (Processing / p5.js / つぶやきGLSL) の自動判定。書き分けの指定は
  要らない

## 0.2.4 — 2026-08-14

### 追加

- **ログ** `<data>/logs/tsubu.log`。1 行 1 件、行の頭は `時刻 レベル 種別` で、
  作品にまつわる行は `鍵=値` を並べる。人だけでなくエディタや外部の
  エージェントが読んで直しどころを見つけられる形にしてある。1 MiB で 3 世代
  まで回す。詳しさは `RUST_LOG` で変える
- 明かりを実装。`ambientLight()` / `directionalLight()` / `pointLight()`、
  1 フレームに 5 つまで。**色はそのまま面に乗る**ので、黄色い明かりを当てた
  白い立体は黄色くなる。位置と向きに掛けるのはカメラの分だけ (p5.js に合わせる)
- `rect(x, y, w)` は正方形 (p5)。`smooth()` / `noSmooth()` は受けて何もしない
- `--version` で版とログの置き場を出す

### 修正

- 1 フレームの図形量に上限を置き、超えたら作品を止めて理由を出す。**上限を
  超えた確保を GPU へ頼まない**のが肝心で、頼むとデバイスが検証で撥ね、
  wgpu の既定の扱いではプロセスごと落ちる
- p5.js のローカル変数。引数と、その関数の中だけで使われる `let` / `const` /
  `var` がローカルになる。再帰する関数がループ変数を壊さなくなった

## 0.2.3 — 2026-08-14

### 修正

- キャンバスを当てにする作品 — 静的モードの作品と、`f++ || background(0)` の
  ように最初の 1 フレームだけ地を塗る書き方 — を、キャンバスが捨てられたら
  頭から動かし直す。そうしないと白い地に白い線を引き続けることになる
- int ひとつを詰めた色 (`0xAARRGGBB`) として読むのは Processing のときだけに
  した。p5.js に この決まりは無く、当てると `stroke(500)` の alpha が 0 に
  なって何も描かれない
- 立体の稜線の太さを、画面のピクセルではなくキャンバスの単位にした。窓が
  大きいほど網目が細るのを止める

### 追加

- `sphereDetail()`。球の分割は半径によらず 24 × 16 (p5 の既定)。隣り合う面で
  共有する辺は 1 本ぶんしか引かない

## 0.2.2 — 2026-08-14

### 追加

- `arc()` の 7 つめで閉じ方 (`OPEN` / `CHORD` / `PIE`)
- `blendMode()` に `BLEND ADD MULTIPLY SCREEN DIFFERENCE EXCLUSION DARKEST
  LIGHTEST SUBTRACT REPLACE`。`DIFFERENCE` は除外で近似する (GPU の合成は
  引き算の符号を選べないため)
- `drawingContext` の影

### 修正

- `beginShape()` の塗りを耳切り法にした。扇状に分けると凹んだところが外へ
  はみ出す

## 0.2.1 — 2026-08-14

### 修正

- **描画状態を作品ごとに預けるようにした。** `Graphics` はギャラリー全体で
  1 つを使い回すのに `setup()` は一度きりしか走らないので、そこで決めたこと
  (`stroke(-1)`、`size()`、`colorMode(HSB)`、3D かどうか) が、他の作品のために
  状態を初期化した瞬間に永久に失われていた。先読みはこれを悪化させる —
  `setup()` が別の `Graphics` の側で走ってしまうため

### 追加

- 画面への収め方 (収める / 埋める)。サムネイルにも同じ設定が効くので、
  カードと Viewer の見え方が揃う
- `I` に CPU 負荷、作品の実行時間、フレームあたりの命令数と三角形
- p5.js の配列とそのメソッド (`map` `filter` `reduce` など)、展開 (`...`)
- `pushStyle()` / `popStyle()`。p5 の `push()` / `pop()` は座標変換と見た目の
  両方を退避する
- 記号フォントの取り回し。どの記号フォントを使うかで絵が変わるため

## 0.2.0 — 2026-08-14

### 追加

- 作者とリンク。`O` かリスト表示の ↗ で既定のブラウザが開く。**開くのは
  `http://` と `https://` だけ** — リンクは作品と一緒に配られてくる値で、
  それを外部のプログラムへ渡すことになる
- 検索欄。タイトル・id・作者の部分一致
- **静的モード。** `setup()` も `draw()` も書かず、関数の外に文を並べただけの
  作品も動く。つぶやきの短いコードはこの形が多い
- **3D。** `size(w, h, P3D)` / `createCanvas(w, h, WEBGL)`、`box()` `sphere()`
  `rotateX() rotateY() rotateZ()` `lights()`
- Java Mode の配列 (`new float[r][c]`、`{1,2,3}`、拡張 for)、複合代入、
  1 文に複数の宣言
- 色の成分の取り出し (`red()` `hue()` など)、`clear()`、`resetMatrix()`
- `I` の情報表示

## 0.1.0 — 2026-08-13

最初の版。

- Gallery (格子とリスト、検索、絞り込み、並べ替え)、Viewer (全画面再生、
  スライドショー、スクリーンセーバー)、Editor (色分け、エラー表示)、設定
- Processing Lite ランタイム。lexer → parser → AST → bytecode → VM
- wgpu の Renderer。Viewer とサムネイル生成が同じ経路を通る
- SQLite の作品目録、多重起動の防止、日英の切り替え
